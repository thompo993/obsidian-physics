<< [[<% tp.date.now("YYYY-MM-DD", -1) %>|yesterday]] || [[<% tp.date.now("YYYY-MM") %>|month]] || [[<% tp.date.now("YYYY-MM-DD", 1) %>|tomorrow]] >>

# <% tp.date.now("dddd, MMMM Do, YYYY") %>

> [!quote] Those who will play with cats must expect to be scratched.
> — Cervantes

## Agenda

> [!todo]+ Today
> ```tasks
> not done
> happens <% tp.date.now("YYYY-MM-DD") %>
> hide recurrence rule
> hide due date
> hide scheduled date
> sort by priority
> ```

> [!danger]+ Overdue 
> ```tasks
> not done
> (due before <% tp.date.now("YYYY-MM-DD") %>) OR ((happens before <% tp.date.now("YYYY-MM-DD") %>) AND (priority is above none))
> hide recurrence rule
> sort by due date
> ```

> [!tip]- Next two weeks
> ```tasks
> not done
> happens after <% tp.date.now("YYYY-MM-DD") %>
> happens before <% tp.date.now("YYYY-MM-DD", 14) %>
> hide recurrence rule
> hide due date
> hide scheduled date
> group by happens
> ```